name: Apply Event Detail Lineup Games

on:
  pull_request:
    paths:
      - .github/workflows/apply-event-detail-lineup-games.yml
      - scripts/apply-event-detail-lineup-games.py

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0
      - uses: actions/setup-node@v6
        with:
          node-version: 24
      - name: Apply event-detail game ID correction
        run: python scripts/apply-event-detail-lineup-games.py
      - name: Validate implementation
        run: |
          npm install
          npm run check
          npm run build:web
      - name: Commit generated correction
        shell: bash
        run: |
          if git diff --quiet; then
            echo "Event-detail lineup correction already applied."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add packages/adapter-lol/src/riot-resolved-provider.ts packages/adapter-lol/src/riot-resolved-provider.test.ts
          git commit -m "Resolve lineup games from Riot event details"
          git push origin HEAD:${{ github.head_ref }}
