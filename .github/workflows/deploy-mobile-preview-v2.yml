name: Retired Mobile Preview V2

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This duplicate workflow is retired; deploy-mobile-preview.yml owns the mobile preview."
