name: Retired Mobile Pull Request Preview

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Mobile deployments are owned by deploy-mobile-demo.yml and apps/web-mobile."
