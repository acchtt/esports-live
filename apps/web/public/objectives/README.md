# Objective assets

These images are Riot Games in-client HUD assets extracted and served by CommunityDragon. They are stored locally so the production UI does not depend on a third-party asset host at runtime.
