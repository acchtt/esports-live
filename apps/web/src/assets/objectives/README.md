# Objective assets

Riot scoreboard/broadcast objective glyphs sourced through CommunityDragon. Tower, dragon, Baron, and inhibitor are isolated connected components from `scoreboardatlas.png`; Rift Herald uses Riot's `_riftherald.png`. The web build imports these files through Vite so production uses fingerprinted asset URLs.
